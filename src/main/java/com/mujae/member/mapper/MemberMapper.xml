<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mujae.member.dao.MemberDAO">

	<select id="idCheck" resultType="int">
		select count(id) from user_info
		where id = #{id}
	</select>
	<!--회원가입-->
	<insert id="memberJoin" parameterType="mdto">
		INSERT INTO user_info
		    (ID, PWD, NAME, EMAIL, PHONE)
		VALUES
		    (#{id},#{pwd},#{name},#{email},#{phone})
	</insert>
	<!--화원정보 가져오기-->
	<select id="getMember" parameterType="mdto" resultType="mdto">
		SELECT
		    ID, NAME, PWD, EMAIL,PHONE
		FROM user_info
		WHERE ID = #{id} and PWD = #{pwd}
	</select>
	<!--아이디찾기-->
	<select id="searchId" parameterType="mdto" resultType="string">
		SELECT ID FROM user_info
		WHERE NAME= #{name} AND EMAIL=#{email}
	</select>
	<!--비밀번호 수정-->
	<update id="updatePasswd" parameterType="mdto">
		UPDATE user_info
		SET PWD = #{pwd}
		WHERE ID = #{id}

	</update>
	<!--회원정보 수정-->
	<update id="memUpProc" parameterType="mdto">
		UPDATE user_info
		SET
		    PWD  = #{pwd}
			,EMAIL = #{email}
			,PHONE = #{phone}
		WHERE ID =  #{id}
	</update>
	<!--탈퇴하기-->
	<delete id="memDelete"  parameterType="mdto">
		DELETE
		FROM user_info
		WHERE ID = #{id}
	</delete>
</mapper>